# Velocit Manager v0.1.2

## อัปเดตคีย์โปรแกรม

- เพิ่มการแสดงระยะเวลาคีย์ Rejoin ที่เหลือต่อท้ายสถานะ Connected
- หากคีย์มีค่าเหลือ 0 วัน จะแสดงเป็น `Unlimited`
- ระบบยังคงตรวจสอบคีย์และอัปเดตสถานะอัตโนมัติเมื่อเปิดโปรแกรม

## ธีมโปรแกรม

- เพิ่มปุ่มสลับธีมสว่างและธีมมืด
- ปรับองค์ประกอบหลักของโปรแกรมให้เปลี่ยนตามธีม รวมถึงหัวตาราง Accounts

## หน้า Tools

- ย้าย Solver Captcha In-game ไปไว้ที่หน้า Tools
- จัดวางพื้นที่หน้า Tools เป็นสองฝั่งตามรูปแบบใหม่

## การกำหนดสเปคหน้าจอ Roblox

- เพิ่มการตั้งค่า ROBLOX CPU SETTINGS ในหน้า Manager
- เลือกการใช้ CPU ได้ตั้งแต่ `1C` ถึง `5C`
- เลือกการใช้ RAM ได้ตั้งแต่ `1R` ถึง `4R`
- เปลี่ยน CPU/RAM เป็น custom dropdown ของ Velocit Manager
- ปรับขนาดกรอบให้พอดีกับพื้นที่และไม่ล้นออกนอก sidebar

## การเลือกเซิร์ฟเวอร์

- เพิ่ม Join Low Player สำหรับเลือกเซิร์ฟเวอร์ที่มีผู้เล่นน้อย
- เพิ่ม Join Low Pings สำหรับเลือกเซิร์ฟเวอร์ที่มี ping ต่ำ
- รองรับการเปิดใช้งานทีละโหมด และบันทึกการตั้งค่าไว้

## หมายเหตุ

การอัปเดตอัตโนมัติจะทำงานผ่าน GitHub Releases หลังติดตั้งเวอร์ชั่นที่รองรับระบบ updater แล้ว
