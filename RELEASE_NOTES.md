# Velocit Manager v0.1.5

## แก้ไขปัญหา CMD กระพริบ

- แก้ไขคำสั่งตรวจสอบ process Roblox ที่ทำให้หน้าต่าง CMD เปิด–ปิดซ้ำระหว่างใช้งาน
- ปรับ `tasklist` และ `taskkill` ให้ทำงานแบบเบื้องหลังโดยไม่สร้างหน้าต่าง console
- ลดการรบกวนระหว่างการตรวจสอบ process และการตั้งค่าทรัพยากร Roblox
- คง CMD ของ Auto Relaunch Dashboard ไว้เฉพาะกรณีที่ผู้ใช้เปิดสวิตช์ Auto Relaunch เท่านั้น

## หมายเหตุ

เวอร์ชั่นนี้เป็น Release สาธารณะ ผู้ใช้สามารถดาวน์โหลดและอัปเดตผ่าน GitHub Releases ได้ตามปกติ
