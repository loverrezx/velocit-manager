# Account Manager (Tauri)

## วิธีรัน (ง่ายสุด)

1. แตกไฟล์ไปที่ C:\app\tauri-account-manager
2. เปิด CMD แล้วรัน:

```bash
cd C:\app\tauri-account-manager
npm install
npm run tauri dev
```

ครั้งแรกจะใช้เวลานานหน่อย (compile)
